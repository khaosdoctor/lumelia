import { BotContext } from '../bot.ts'
import { RepositoryList } from '../core/repositories/mod.ts'

export async function settleCommandFactory(repositories: RepositoryList) {
	return async (ctx: BotContext) => {
	}
}
